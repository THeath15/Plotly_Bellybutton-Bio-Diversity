// 12.3.1
// const url = "https://api.spacexdata.com/v2/launchpads";

// d3.json(url).then(receivedData => console.log(receivedData));

// const url = "https://api.spacexdata.com/v2/launchpads";
// d3.json(url).then(data => console.log(data));

// //Code to retrieve the details from Vandenberg Air Force Base
// d3.json(url).then(spaceXResults => console.log(spaceXResults[0]));

// // code to retrieve the full_name of the Vandenberg Air Force
// d3.json(url).then(spaceXResults => console.log(spaceXResults[0].full_name));  

// //Code to access/retrieve the latitude of Vandenberg Air Force
// d3.json(url).then(spaceXResultslat => console.log(spaceXResultslat[0].location.latitude));  
// d3.json(url).then(spaceXResultslong => console.log(spaceXResultslong[0].location.longitude));  

// //SKILL DRILL - Use map() to print only the latitude and longitude coordinates of each SpaceX launch station

// 12.3.2
const url = "https://api.spacexdata.com/v2/launchpads";
d3.json(url).then();

const url = "https://api.spacexdata.com/v2/launchpads";
d3.json(url).then();